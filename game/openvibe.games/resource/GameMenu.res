"GameMenu"
{
	// Intentionally empty: the stock GameUI menu must be invisible — the
	// OpenVibe HTML panel is the only menu. If the HTML panel ever fails to
	// load, launch with -console and use ov_menu / ov_join <mode> directly.
}
